services:
  nanoapi:
    build:
      context: .
      dockerfile: Dockerfile
    image: nanoapi:local
    container_name: nanoapi
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data

